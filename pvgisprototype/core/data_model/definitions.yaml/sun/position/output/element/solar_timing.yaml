name: SolarTimingOutputStructureElement
label: Solar Timing Algorithm
type: OutputStructureElement
description: Output structure element for the solar timing algorithm
symbol: 🧩

sections:

  output:
    structure:

      - section: Context
        description: Contextual section
        condition: verbose > 1
        subsections:

          - subsection: Solar Timing Algorithms
            id: SolarTimingOutputStructureElement
            description: Solar timing algorithm
            condition: verbose
            fields:
              - solar_timing_algorithm

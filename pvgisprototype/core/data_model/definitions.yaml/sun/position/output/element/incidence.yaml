name: SolarIncidenceOutputStructureElement
label: Solar Incidence
type: OutputStructureElement
description: Output structure element for the solar incidence data model
symbol: ⭸ 🧩

sections:

  output:
    structure:

      - section: Context
        description: Contextual section
        condition: verbose > 1
        subsections:

          - subsection: Solar Position
            id: SolarIncidenceOutputStructureElement
            description: Solar incidence parameter
            condition: verbose > 2
            fields:
              - solar_incidence

          - subsection: Solar Position Algorithms
            description: Solar position algorithms
            condition: verbose >= 2
            fields:
              - solar_incidence_model
              - solar_incidence_definition

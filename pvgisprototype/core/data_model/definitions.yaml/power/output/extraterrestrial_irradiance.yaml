name: ExtraterrestrialIrradianceOutputStructureElement
description: Output structure element for the extraterrestrial irradiance

fields:

  presentation:
    structure:

      - section: Context
        description: Contextual section
        condition: verbose > 1
        subsections:

          - subsection: Extraterrestrial Irradiance
            description: Extraterrestrial irradiance subsection
            fields:
              - extraterrestrial_normal_irradiance

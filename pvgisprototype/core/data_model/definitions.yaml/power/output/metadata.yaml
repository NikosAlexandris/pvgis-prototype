name: MetadataOutputStructureElement
description: Output structure element for the metadata

sections:

  output:
    type: dict
    initial:
    structure:

      - section: Metadata
        description: Metadata section
        condition: verbose > 2
        fields:
          - photovoltaic_module_type
          - technology
          - peak_power
          - power_model
          - solar_radiation_model

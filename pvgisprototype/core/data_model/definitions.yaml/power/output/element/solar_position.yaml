name: SolarPositionOutputStructureElement
label: Solar Position
type: OutputStructureElement
description: Output structure element for the solar position parameters
symbol: ⯐ 🧩

sections:

  output:
    type: dict
    initial:
    structure:

      - section: Context
        id: SolarPositionOutputStructureElement
        description: Contextual section
        condition: verbose > 1
        subsections:

          - subsection: Solar Position
            description: Solar position parameters
            condition: verbose > 8
            fields:
              - sun_horizon_position
              - solar_incidence
              - solar_azimuth
              - solar_altitude

name: FingerprintOutputStructureElement
label: Fingerprint
type: OutputStructureElement
description: Output structure for the Fingerprint attribute
symbol: 🆔🧩
color: lightgray

sections:

  output:
    type: dict
    initial:
    structure:

      - section: Fingerprint
        id: FingerprintOutputStructureElement
        condition: fingerprint is True
        fields:
          - fingerprint

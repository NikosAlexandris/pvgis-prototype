name: SystemLossOutputStructureElement
label: System Loss
type: OutputStructureElement
description: Output structure element for the System loss
symbol: '- 🧩'
color: gray

sections:

  output:
    type: dict
    initial:
    structure:

      - section: Context
        id: SystemLossOutputStructureElement
        description: Contextual section
        condition: verbose > 1
        subsections:

          - subsection: System Loss
            description: System loss
            condition: verbose > 2
            fields:
              - efficiency_factor
              - system_efficiency

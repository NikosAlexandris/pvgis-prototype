name: SystemLossOutputStructureElement
label: System Loss
type: OutputStructureElement
description: Output structure element for the system loss of a photovoltaic module
symbol: '- 🧩'
color: gray

sections:

  output:
    structure:

      - section: Context
        id: SystemLossOutputStructureElement
        description: Contextual section
        condition: verbose > 1
        subsections:

          - subsection: System Loss
            description: System loss
            condition: verbose > 2
            fields:
              - system_efficiency # subtracted from 1 = system loss
              - efficiency_factor

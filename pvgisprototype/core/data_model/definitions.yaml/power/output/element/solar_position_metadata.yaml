name: SolarPositionMetadataOutputStructureElement
label: Solar Position Metadata
type: OutputStructureElement
description: Output structure element for the solar position metadata
symbol: 🧩
color: darkorange

sections:

  output:
    type: dict
    initial:
    structure:

      - section: Context
        id: SolarPositionMetadataOutputStructureElement
        description: Contextual section
        condition: verbose > 1
        subsections:

          - subsection: Solar Position Metadata
            description: Solar position parameters
            condition: verbose > 3
            fields:
              - solar_timing_algorithm
              - solar_positioning_algorithm
              - azimuth_origin
              - solar_incidence_model
              - solar_incidence_definition
              - sun_horizon_positions

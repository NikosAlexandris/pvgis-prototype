name: HorizontalIrradianceOutputStructureElement
description: Output structure element for the horizontal irradiance components

sections:

  output:
    type: dict
    initial:
    structure:

      - section: Context
        description: Contextual section
        condition: verbose > 1
        subsections:

          - subsection: Horizontal Irradiance
            description: Horizontal irradiance components
            condition: verbose > 6
            fields:
              - diffuse_horizontal_irradiance
              - direct_horizontal_irradiance
              - extraterrestrial_horizontal_irradiance

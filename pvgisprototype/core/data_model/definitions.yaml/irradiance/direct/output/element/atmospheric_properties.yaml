name: AtmosphericPropertiesOutputStructureElement
label: Atmospheric Properties
type: OutputStructureElement
description: Output structure for the atmospheric properties in the direct horizontal irradiance data model
symbol:

sections:

  output:
    structure:

      - section: Context
        description: Contextual section
        condition: verbose > 1
        subsections:

          - subsection: Atmospheric Properties
            description: Atmospheric Properties
            condition: verbose > 2
            fields:
              # - linke_turbidity_factor
              - optical_air_mass
              - refracted_solar_altitude

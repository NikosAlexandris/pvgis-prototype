name : ExtraterrestrialHorizontalIrradianceOutput
description: Output structure for the Extraterrestrial horizontal irradiance data model

imports:
  - output/template  # the base structure
  - output/solar_radiation_model

fields:

  presentation:
    type: dict
    initial:
    structure:

      - section: Extraterrestrial Horizontal Irradiance
        description: Core section
        fields:
          - name
          - title
          - description
          - symbol
          - value
          - unit

      - section: Context
        description: Contextual section
        condition: verbose > 1
        subsections:

          - subsection: Irradiance Metadata
            description: Irradiance components
            fields:
              - normal

name: InclinedIrradianceReflectivityEffectOutputStructureElement
label: Inclined Irradiance Reflectivity Effect
type: OutputStructureElement
description: Output structure element for the reflectivity effect of inclined irradiance components
symbol: ⸾ ∡ 🧩

sections:

  output:
    structure:

      - section: Context
        description: Contextual section
        condition: verbose > 1
        subsections:

          - subsection: Reflectivity effect
            description: Reflectivity effect
            condition: verbose > 3
            fields:
              - direct_inclined_reflected
              - diffuse_inclined_reflected
              - ground_reflected_inclined_reflected

name: ReflectivityEffectOutputStructureElement
label: Reflectivity Effect
type: OutputStructureElement
description: Output structure element for the reflectivity effect
symbol: ⭜ 🧩
color: magenta

sections:

  output:
    type: dict
    initial:
    structure:

      - section: Context
        id: ReflectivityEffectOutputStructureElement
        description: Contextual section
        condition: verbose > 2
        subsections:

          - subsection: Reflectivity effect
            description: Reflectivity effect
            condition: verbose > 6
            fields:
              - global_inclined_reflected
              - direct_inclined_reflected
              - diffuse_inclined_reflected
              - ground_reflected_inclined_reflected

name: ReflectivityFactorOutputStructureElement
label: Reflectivity Factor
type: OutputStructureElement
description: Output structure element for the reflectivity factor of an inclined irradiance data model
symbol: ⸾ ⇣ ∡ 🧩

sections:

  output:
    structure:

      - section: Context
        description: Contextual section
        condition: verbose > 1
        subsections:

          - subsection: Reflectivity factor
            id: ReflectivityFactorOutputStructureElement
            description: Reflectivity factor
            fields:
              - reflectivity_factor
              - value_before_reflectivity

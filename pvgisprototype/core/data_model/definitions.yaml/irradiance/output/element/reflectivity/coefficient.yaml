name: ReflectivityCoefficientOutputStructureElement
label: Reflectivity Coefficient
type: OutputStructureElement
description: Output structure element for the reflectivity coefficient of an sky-diffuse inclined irradiance data model
note: This is an extra quantity not identical to the reflectivity factor !
symbol: 🧩

sections:

  output:
    structure:

      - section: Context
        description: Contextual section
        condition: verbose > 1
        subsections:

          - subsection: Reflectivity Coefficient
            description: Surface reflectivity coefficient
            condition: verbose > 1
            fields:
              - reflectivity_coefficient

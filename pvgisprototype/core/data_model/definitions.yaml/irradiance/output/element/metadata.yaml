name: SolarIrradianceMetadataOutputStructureElement
label: Solar Irradiance Metadata
type: OutputStructureElement
description: Output structure element for solar irradiance metadata
symbol: 🧩
color: lightmagenta

sections:

  output:
    structure:

      - section: Metadata
        id: IrradianceMetadata
        description: Solar irradiance metadata
        condition: verbose > 2
        fields:
          - solar_radiation_model

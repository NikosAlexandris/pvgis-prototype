name: IrradianceMetadataOutputStructureElement
label: Irradiance Metadata
type: OutputStructureElement
description: Output structure for the irradiance metadata in the direct horizontal irradiance data model
symbol: ⸾ 🧩

sections:

  output:
    structure:

      - section: Context
        description: Contextual section
        condition: verbose > 1
        subsections:

          - subsection: Irradiance Metadata
            description: Irradiance components
            condition: verbose > 2
            fields:
              - direct_normal_irradiance

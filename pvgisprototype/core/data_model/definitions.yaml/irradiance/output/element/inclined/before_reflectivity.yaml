name: InclinedIrradianceBeforeReflectivityOutputStructureElement
label: Inclined Irradiance Before Reflectivity
type: OutputStructureElement
description: Output structure element for the inclined irradiance components before reflectivity
symbol: ⸾ ∡ 🧩

sections:

  output:
    structure:

      - section: Context
        description: Contextual section
        condition: verbose > 1
        subsections:

          - subsection: Value before reflectivity
            description: Inclined irradiance before reflectivity
            fields:
              - value_before_reflectivity
              - direct_inclined_before_reflectivity
              - diffuse_inclined_before_reflectivity
              - ground_reflected_inclined_before_reflectivity

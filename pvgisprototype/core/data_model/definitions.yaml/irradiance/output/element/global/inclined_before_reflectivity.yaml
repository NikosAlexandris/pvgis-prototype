name: GlobalInclinedIrradianceBeforeReflectivityOutputStructureElement
label: Global Inclined Irradiance Before Reflectivity
type: OutputStructureElement
desription: Output structure element for the global inclined irradiance before reflectivity

sections:

  output:
    structure:

      - section: Context
        id: GlobalInclinedIrradianceBeforeReflectivityOutputStructureElement
        description: Contextual section
        condition: verbose > 1
        subsections:

          - subsection: Global Inclined irradiance before reflectivity
            description: Global inclined irradiance before reflectivity loss
            condition: verbose > 5
            fields:
              - global_inclined_before_reflectivity

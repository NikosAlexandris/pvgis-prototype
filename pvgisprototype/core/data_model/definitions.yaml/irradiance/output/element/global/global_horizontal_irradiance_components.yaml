name: GlobalHorizontalIrradianceComponentsOutputStructureElement
label: Global Horizontal Irradiance Components
type: OutputStructureElement
description: Output structure element for the irradiance component dependencies of a global horizontal irradiance data model
symbol: ⸾ ⤋ ⭳
color: yellow

sections:

  output:
    structure:

      - section: Context
        description: Contextual section
        condition: verbose > 1
        subsections:

          - subsection: Irradiance Metadata
            description: Irradiance components
            condition: verbose > 2
            fields:
              - diffuse_horizontal_irradiance
              - direct_horizontal_irradiance
              - extraterrestrial_normal_irradiance

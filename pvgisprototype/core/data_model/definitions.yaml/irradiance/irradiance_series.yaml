name: IrradianceSeries
description: Definition of a generic solar irradiance time series data model

require:
  - data_model_template
  
  # Possible add more attributes ?

  # - irradiance/attribute/solar_constant
  # - irradiance/attribute/physically_possible_irradiance_limits
  # - attributes/values_out_of_range
  # - metadata/quality
  # - metadata/fingerprint

  - irradiance/output/template

sections:

  name:
    description: Name of the generic solar irradiance time series data model
    initial: Irradiance Data

  shortname:
    description: Short name of generic solar irradiance time series data model

  supertitle:
    description: Supertitle of the generic solar irradiance time series data model. This may be useful in the context of a plot.
    initial: Solar Irradiance

  title:
    description: Title of the generic solar irradiance time series data model

  label:
    description: Label of the generic solar irradiance time series data model. This may be useful in the context of a plot.
    initial: Solar Irradiance

  description:
    description: Description of the data model
    initial: "A solar irradiance component such as global, direct, diffuse sky-reflected, diffuse ground-reflected or extraterrestrial and any of normal, horizontal or inclined, is a solar power component received per unit area at a given moment, expressed in watts per square meter (W/m²). Except for the extraterrestrial and direct normal components, the values concern a specific location and a moment or period in time."

  algorithm:
    description: Description of the calculation

  symbol:
    description: Symbol of the generic solar irradiance time series data model
    initial: ⸾

  value:
    description: Values in form of a NumPy NDArray
    unit: W/m²

  unit:
    description: Unit of the irradiance values
    initial: W/m²

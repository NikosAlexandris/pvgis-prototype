name: DiffuseSkyReflectedInclinedIrradianceOutput
label: Diffuse Sky-Reflected Inclined Irradiance
type: Output Structure
description: Output structure for the clear-sky sky-diffuse inclined irradiance
symbol: ⸾ 🗤 ∡

require:
  - irradiance/diffuse/sky_reflected/output/inclined_template

sections:

  output:
    type: dict
    initial:
    structure:

      - section: Context
        description: Contextual section
        condition: verbose > 1
        subsections:

          - subsection: Surface Position
            description: Surface position parameters
            condition: verbose
            fields:
              - elevation
              - surface_orientation
              - surface_tilt
              - shading_algorithm
              - angle_output_units

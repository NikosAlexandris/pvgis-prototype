name: SurfaceInShadeMinimalOutputStructureElement
label: Surface-in-Shade Output
type: OutputStructureElement
description: Output structure element for the SurfaceInShade state of data model.
symbol: 🮞  🧩
color: orange

sections:

  output:
    structure:

      # - section: Context
      #   description: Contextual section
      #   condition: verbose > 1
      #   subsections:

      - section: Shading
        id: SurfaceInShadeMinimalOutputStructureElement
        description: Shading parameters  
        condition: verbose > 1
        fields:
          - surface_in_shade

      - section: Shading Metadata
        id: SurfaceInShadeMinimalOutputStructureElement
        description: Shading metadata
        condition: verbose >= 2
        fields:
          - shading_algorithm

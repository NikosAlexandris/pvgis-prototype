name: SurfaceInShadeOutputStructureElement
label: Surface-in-Shade Output
type: OutputStructureElement
description: Output structure element for the SurfaceInShade state of data model.
symbol: 🮞  🧩
color: orange

sections:

  output:
    structure:

      - section: Shading
        id: SurfaceInShadeOutputStructureElement
        description: Shading parameters  
        condition: verbose > 1
        fields:
          - shading_state
          - surface_in_shade
          - visible

      - section: Shading Metadata
        id: SurfaceInShadeOutputStructureElement
        description: Shading metadata
        condition: verbose >= 2
        fields:
          - shading_algorithm
          - shading_states

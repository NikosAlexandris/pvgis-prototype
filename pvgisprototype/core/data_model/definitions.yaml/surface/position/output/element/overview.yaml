name: SurfacePositionOutputStructureElement
label: Surface Position Output
type: OutputStructureElement
description: Output structure element for the surface position parameters of a data model
symbol: ⯐ 🧩
color: purple

sections:

  output:
    structure:

      - section: Surface Position
        id: SurfacePositionOutputStructureElement
        description: Surface position parameters
        condition: verbose  # single quantities (not time series!) appearing once
        fields:
          - surface_orientation
          - surface_tilt
          - angle_output_units

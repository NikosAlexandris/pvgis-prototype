# Normalize line endings
* text=auto

# Git LFS for large files
*.cast filter=lfs diff=lfs merge=lfs -text
*.nc   filter=lfs diff=lfs merge=lfs -text
docs/images/pvgis6_snapshot_of_architecture_graph_on_20241022T122734.jpg filter=lfs diff=lfs merge=lfs -text

# Ensure other common images are binary (optional)
*.png binary
*.jpg binary
